# Kopiere diese Datei nach .env und passe die Werte an.

# Gemeinsames Geheimnis zwischen KG-Tracker und Generator (Pflicht).
AIRLOCK_API_KEY=bitte-langes-zufaelliges-secret-einsetzen

# Maximale Anzahl Locks pro Batch.
AIRLOCK_MAX_BATCH=200

# Dashboard-Komfort: 1 = Server spritzt den API-Key automatisch ins Dashboard,
# sodass es ohne Eingabe verbindet. ACHTUNG: Der Key ist dann im Seitenquelltext
# sichtbar -> nur in einem vertrauten LAN aktivieren. 0 = aus (Standard).
AIRLOCK_UI_AUTOKEY=0

# NFC-Signierung: Geheimnis fuer HMAC(Code|Tag-UID). PFLICHT im Betrieb.
# Dasselbe Secret braucht spaeter der KG-Tracker zum Offline-Verifizieren der
# Tags (oder er ruft den /nfc/verify-Endpunkt des Generators auf).
AIRLOCK_NFC_SECRET=bitte-eigenes-langes-nfc-secret-einsetzen

# Optionaler statischer KG-Tracker-Key (eingeschraenkt: nur Lesen, Statuswechsel,
# NFC-verify). Normalerweise NICHT noetig – erzeuge die Keys lieber im Dashboard
# unter "KG-Tracker" (dort einmalig anzeigbar, widerrufbar, mit Zugriffs-Log).
# Leer lassen = nur die im Dashboard erzeugten Keys gelten.
AIRLOCK_KG_API_KEY=

# BETA: erlaubt beim bewussten Neu-Verheiraten (rebind=true) auch, einen Tag von
# einem ANDEREN Schloss "wegzunehmen" (dort loesen, hierher umbinden). Nur fuer
# die Beta-Phase. In Produktion auf 0/leer lassen -> ein Tag bleibt dann dauerhaft
# an hoechstens einem Schloss. Der normale rebind (Bindung eines Schlosses durch
# einen FREIEN Tag ersetzen) haengt NICHT an diesem Flag. 0 = aus (Standard).
AIRLOCK_BETA_TAG_MOVE=0
